> 현재 구현은 [FAKEDOOR_V52_HANDOFF.md](FAKEDOOR_V52_HANDOFF.md)를 참고하세요. 아래는 이전 v5.1 이력입니다.

# Mio v5.1 — UI 개선 + LLM 채팅 목업

현재 버전: 2026-09-15. v5 인수인계 문서는 이전 스냅샷이며, 이 문서가 현재 동작을 설명한다. 실제 LLM·신청·서버 전송은 연결하지 않았다.

## 화면 변경

- 랜딩의 짙은 보라색을 유지하고 이용 안내·필요 선택·채팅·정리·지원·종료는 밝은 라벤더로 전환했다. 본문은 짙은 자두색, CTA는 보라색, 지원 정보는 청록 보조색이다.
- 모바일 랜딩 캐릭터 영역과 필요 카드를 축소했다. 영문 장식 문구 일부를 한국어로 바꾸고 보조 글씨와 대화 본문의 대비·크기를 높였다.
- 채팅의 예시 선택과 직접 입력을 한 입력창으로 통합했다. 채팅 화면에서 `/tone-guide`를 새 탭으로 열 수 있다.

## 목업 동작

`src/components/mock-chat-composer.tsx`의 입력창은 1,000자까지 가상 문장을 받는다. 한글 조합 중 Enter를 전송으로 처리하지 않으며, Shift+Enter는 줄바꿈이다. 공백 입력과 응답 진행 중 중복 전송은 막는다.

`src/lib/chat-mock.ts`의 공개 가상 예시와 정확히 일치하면 대응하는 고정 답변을 표시한다. 다른 문장은 해석하지 않으며 선택한 방향에 따른 일반 예시를 표시한다. 의미 분석·실제 생성·위험 감지 기능은 없다. 이 한계를 시작 안내와 채팅 화면에 표시한다.

목업 상태:

1. 입력을 현재 화면에 추가.
2. 850ms의 응답 대기 시뮬레이션.
3. 고정 답변을 6자씩 표시하는 스트리밍 시뮬레이션.
4. 완료된 답변만 대화 기록에 추가.
5. 중단하면 진행 중 표시를 지우고 같은 응답을 다시 볼 수 있음. 재시도는 사용자 입력을 중복 추가하지 않음.
6. 「목업 상태 테스트」에서 다음 응답을 실패하도록 설정할 수 있음. 오류를 실제 서버 장애로 설명하지 않음.
7. 화면 이동·방향 변경 시 진행 중 작업 취소. 완료된 메시지는 유지. 미전송 초안과 실패·중단 상태는 채팅 화면을 떠나면 초기화.

## 데이터·지표 구분

- 직접 입력 원문은 React 메모리에만 유지한다. 서버·localStorage·세션 저장소로 전송/저장하지 않는다. 종료·처음으로·새로고침 시 지운다.
- 정리·다운로드는 공개 예시로 표시한 문장만 포함하며 직접 입력 원문을 제외한다. 정리 편집 기능으로 사용자가 직접 추가한 내용은 다운로드 대상이므로 관련 안내는 계속 유지한다.
- `source: fixture | typed | mock`으로 메시지를 구분한다. 타이핑한 문장이 공개 예시와 정확히 같으면 fixture로 기록한다. 개인적 자기노출 여부를 판정하는 분류가 아니다.
- 버전 `need-flow-v5.1`, 저장 키 `mio_need_flow_events_v5_1`. v4와 v5 기록은 건드리지 않는다. `/prototype-review`는 v5.1만 읽는다.
- 입력 발생은 `mock_input_sent`, 예시 전송은 `mock_fixture_sent`. 응답 상태는 `mock_response_started/completed/stopped/failed/retried`로 구분한다. 모두 `scripted_demo`이며 실제 AI 성능·신뢰·가치·사용자 피해·리텐션 지표가 아니다.
- 원문은 이벤트 속성에 넣지 않는다. 저장 차단은 체험을 막지 않는다. 내부 테스트는 `?internal=1`로 제외할 수 있다.

## 대화 톤의 설계 근거와 한계

기준은 기존 Phase5 `MIO_RESEARCH_ALIGNMENT_AND_NEXT_VALIDATION.md` 및 사례 원장이다. 연구 파일은 수정하지 않았다. 아래는 기록의 해석이며 사람 수나 원인의 인과적 추정이 아니다.

| 기존 근거 | 반영한 설계 가설 | 단정하지 않는 것 |
|---|---|---|
| C03: 기계적 공감·해결 안 됨, AI 중단 자기보고 | 상투적 위로를 반복하지 않고 사용자가 말한 내용을 짧게 반영 | 짧은 답이면 유용하다는 인과 효과 |
| C22: 편들기에 대한 불신 | 감정 인정과 상대방 의도·사실 판단을 구분 | 중립적 말투만으로 신뢰를 확보한다는 주장 |
| C08: 도움과 재각인 부담·중단이 공존 | 질문을 줄이거나 종료할 선택, 불편을 반박하지 않음 | 대화가 항상 도움이 되거나 항상 해롭다는 주장 |
| C14: 상황 이해 목적, 허무함·중단 | 듣기·정리·정보 중 원하는 도움을 확인 | 공감 또는 정리를 모두에게 의무화 |
| H7: 정확한 추가 행동 대비 직접 근거가 부족 | 과제와 다음 행동을 강제하지 않음 | 행동 기능의 효과가 없다는 결론 |

제안 말투: 차분한 해요체, 짧은 1~3문장, 필요한 경우 질문 하나, 사용자 발언 범위 안의 반영, 틀린 이해의 수정. 이는 제품 설계 휴리스틱이며 검증된 최적 문장 수·말투가 아니다.

피할 패턴: 진단 단정, 상대방 비난에 동조, 감정 추정 고집, 자동 CBT 과제, 반복 자기노출 요구, 「나만 믿어」「항상 기다릴게」 같은 배타적 관계·상시 돌봄 약속, 실제 전문가 자격 암시. 모든 턴에 질문을 붙이지 않는다.

`/tone-guide`와 `TONE_EXAMPLES`는 팀이 새로 작성한 예시 5개(경청·정리·판단 유보·이해 수정·중단 존중)와 각각의 설계 의도·피할 문장을 보여준다. 인터뷰 직접 인용이나 모델 출력으로 제시하지 않는다.

실제 LLM 관찰에서 확인할 것: 이해가 맞았는지, 사용자가 수정했을 때 반영했는지, 질문이 부담이었는지, 원하는 도움과 응답이 맞았는지, 구체적으로 도움/무도움/불편이 있었는지, 중단·다른 방법을 선택할 수 있었는지. 호감·메시지 길이·대화 지속을 치료 효과나 자기노출로 환산하지 않는다.

## 담당자 연동

- 사용자/UI: 밝은 화면과 모바일 진입 순서, 예시 대화와 톤 가이드 피드백.
- 하늘/FE: 목업 상태와 실제 API 상태를 분리하고 스트리밍·AbortController·재시도·중복 방지·오류 안내를 연동. mock fixture를 실제 모델 응답으로 오표시하지 않기.
- 효찬/BE: 모델 호출·키·응답 중단·요청 식별·서버 검증·비용 한도·오류·데이터 취급 구현. 실제 모델 시스템 지침과 상황별 대응은 별도 검토. 현재 목업의 문자열 일치 분기를 의미 분석이나 안전 장치로 재사용하지 않기.
- 실제 LLM 전환은 새 버전/모드로 운영하고 고지와 지표를 함께 변경. 기존 로컬 입력·이벤트 자동 소급 업로드 금지.

## 실행

기존 `http://localhost:3100`에서 확인 가능. 로컬 의존성 심볼릭 링크 때문에 새로 실행할 때는 `npm run dev -- --webpack`, 빌드는 `npm run build -- --webpack`. 검사: `npm run lint`, `npm run test`.

이번 버전의 검증과 스크린샷 위치: `../output/fakedoor-v5.1/`. 이전 `../output/fakedoor-v5/` 검증은 과거 버전 결과로 보존한다. 외부 배포는 수행하지 않았다.

검증 결과: ESLint 및 테스트 13개 통과. 브라우저에서 입력·대기·스트리밍·중단·재시도·오류·화면 이탈 취소·원문 분리·모바일 390px/320px를 확인했다. 주요 단색 글자/배경 조합의 계산 대비는 본문 13.96:1, 보조 본문 5.76:1, 주요 버튼 5.33:1이다. 전체 접근성 적합성 판정은 아니다.

## 이용 안내 및 동의 UI 수정 — 2026-09-15
- 이용 안내를 사용자 관점의 역할·정보 처리·중단 권리로 변경. 예시 체험 확인 및 안내 화면의 지원 정보 우회 버튼 제거.
- 이용 안내 확인과 민감정보 수집·이용 동의 체크를 분리, 기본 미선택. 두 항목 모두 선택해야 입력 화면 진행. 시작을 다시 누르면 초기화.
- 현 목업의 처리 항목·목적·메모리 유지 기간·외부 미전송·거부 결과를 펼침 안내로 표시. 개발 미리보기 및 가상 입력 안내 유지.
- 현재 체크는 UI 확인용으로 서버에 동의 증적을 저장하지 않는다. notice_acknowledged 이벤트는 실서비스 동의 증적이 아니다.
- 실제 LLM 연동 전 효찬/BE와 운영 주체, 실제 수집 항목/목적/기간, 위탁 및 국외 이전 여부·적용 고지, 삭제/철회 경로를 확정하고 문구를 교체해야 한다. 별도 민감정보 동의를 일반 동의와 합치지 않는다. 서버에서 동의 버전·시각·범위를 검증하고 동의 전 전송을 차단해야 한다. 목업 체크를 이전 동의로 재사용하지 않는다.
- 근거: 개인정보 보호법 제23조 https://www.law.go.kr/lsLinkCommonInfo.do?lsJoLnkSeq=1006185763 . LLM 사용 자체가 아니라 건강 등 민감정보 처리에 관한 별도 동의 요건을 구분한다.

## v1.1 사용자 제공 문서 반영 — 2026-09-15 (앞선 동의 UI 메모 대체)
- 적용 원본: docs/legal-v1.1-source.md. 이전 v0.9를 화면 기준으로 사용하지 않음. 사용자 제공 A의 5개 체크 문구를 사용. 연령/약관/개인정보/민감정보 필수, 마케팅 선택. 기본 미선택. 사용자 요청으로 전체 동의 추가: 연령 확인과 선택 마케팅 포함을 명시하며 개별 해제 가능. 일부 선택 시 중간 상태 표시, 마케팅 미동의는 진행을 막지 않음. 전체 선택·해제·일부 선택·마케팅 해제 동작 브라우저 검증 완료.
- 개인정보 처리방침은 열람 링크만 제공. B의 약관/개인정보/민감정보/마케팅/처리방침을 각각 JSON 블록으로 분리해 상세 모달 및 /legal/{terms,personal,sensitive,marketing,privacy}에 표시. C 내부 메모는 사용자 문서에 포함하지 않음.
- 정식 서비스 기준 문서와 현재 미리보기의 실제 동작을 구분하는 짧은 안내 유지. 동의 선택은 React 상태만 사용. 서버 동의 증적/마케팅 등록/회원가입/외부 AI 전송 없음. 기존 notice_acknowledged는 법적 동의 증적이 아님.
- UI: 데스크톱 2열 소개+동의 카드, 모바일 1열. Pretendard 굵기/크기/간격 조정. 푸터 브랜드 문구 21px(모바일18px), 650 굵기, 두 줄로 표시.
- QA: 1440/390/320px 가로 넘침 없음, 필수4개 및 연령 재해제 차단, 마케팅 미선택 진행, 상세 모달 Escape 및 선택 유지, 5개 문서 경로 확인. 타입 검사 및 Webpack 정적 빌드 통과.
- 원문은 제공받은 정책이며 실제 운영/계약 검증 완료를 의미하지 않음. 개인정보 처리방침의 처리위탁 구역에서 AWS 이름 다음에 AI 처리 업무 및 Abuse Monitoring 보유기간이 연속되어 업체 귀속이 불명확함. 국외 이전 구역에는 OpenAI가 명시됨. 원문을 임의 수정하지 않았으며 BE/운영 담당자가 실제 사업자별 업무와 대조할 항목임. 사업자 주소도 게시 확정 전 문구로 남아 있음.
- 실제 출시 전 서버 동의 버전/시각/철회 기록, 동의 전 API 차단, 삭제 자동화, 외부 이전 고지/계약 및 원문 정책과 실제 인프라 일치를 별도로 구현·검증해야 함. internal=1은 화면 분리용이며 인증이 아님.

- 사용자 요청에 따라 개인정보 처리방침의 미확정 사업자 주소 문구를 화면용 데이터에서 삭제. 제공받은 원본은 보존함.
