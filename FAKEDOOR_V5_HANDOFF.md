# Mio Fake Door v5 — 필요 선택과 화면 체험

> 이전 버전 스냅샷. 현재 구현은 [v5.1 인수인계](FAKEDOOR_V51_HANDOFF.md)를 참조하세요.

구현일: 2026-09-15. 작업 브랜치: `codex/fakedoor-need-flow`.

## 현재 제공하는 것

이 버전은 **고정된 가상 상황으로 탐색하는 UI 프로토타입**이다. 기존 보라색 브랜드 톤과 저장소의 미오 펭귄 자산을 활용했다. 사람형 캐릭터를 고르는 기존 입구를 필요 선택으로 바꿨으며, 이것이 우월하다고 검증된 것은 아니다. 실제 LLM·신청 접수·서버 계측은 연결하지 않았다.

`랜딩 → 이용 안내 → 필요 선택 → 예시 대화 → 선택적 정리 / 지원 정보 / 종료 → 선택적 화면 피드백 → 다음 체험 준비 안내`

- 랜딩·이용 안내·필요 선택에서 지원 정보를 바로 열 수 있다. 대화나 개인정보 입력을 요구하지 않는다.
- 이야기 / 정리 / 아직 모름은 서로 다른 시작 안내와 같은 직장 생활 예시를 사용한다. 현재는 기능별 효용 비교 실험이 아니다.
- 예시 문장 두 개 중 하나를 고르는 단계가 세 번 있다. 개인 사연의 자유 입력이나 AI 응답 생성은 없다.
- 방향 변경은 선택한 예시를 유지한다. 정리 화면은 선택한 문장을 그대로 모으며 감정·문제·다음 행동을 추론하지 않는다.
- 정리 문장을 수정할 수 있고, 수정 뒤 새 예시를 선택하면 추가한 문장도 함께 남긴다. 텍스트 파일 다운로드는 실제로 작동한다.
- 문장과 편집 내용은 메모리에만 둔다. 종료·처음으로·새로고침 시 사라진다. 다운로드한 파일은 기기에 남는다.
- 피드백은 화면 이용 경험에 대한 `편했어요 / 잘 모르겠어요 / 불편했어요`다. 응답하지 않고 종료할 수 있다.
- 다음 체험 현황 버튼은 준비 중 안내로 이어진다. 연락처 입력·신청 성공 화면은 없다.

## 실행과 확인

이 로컬 체크아웃은 `node_modules`가 `/Users/jh/Desktop/mio_funnel/node_modules`를 가리키는 심볼릭 링크다. Next 16.3 기본 Turbopack은 Git 루트 밖의 이 링크를 거부한다. 의존성이나 다른 작업 폴더를 바꾸지 않고 다음 명령으로 실행·검증했다.

```sh
npm run dev -- --webpack --hostname 127.0.0.1 --port 3010
npm run build -- --webpack
npm run lint
npm run test
```

이번 확인은 이미 실행 중이던 `http://localhost:3100` 서버에서 진행했다. 같은 체크아웃의 개발 서버가 실행 중이면 새 서버를 중복 실행하지 않고 기존 주소를 사용한다.

일반 설치를 사용하는 팀원은 저장소의 잠금 파일로 의존성을 설치하고 해당 환경에서 기본 빌드도 확인한다. 이 환경의 우회 때문에 전체 프로젝트 빌드 방식을 변경하지 않았다.

- `/`: 새 프로토타입
- `/prototype-review`: v5 브라우저 로컬 이벤트 확인·내보내기·삭제
- `/review`: 이전 v4 기록용 화면, 기존 그대로 유지. v5를 해석하는 화면이 아니다.
- 내부 테스트는 `/?internal=1`. 리뷰 화면은 내부 테스트를 기본 제외한다.
- 현재 화면 전환은 클라이언트 메모리 상태다. 직접 URL로 특정 단계 진입, 새로고침 복구, 브라우저 뒤로가기로 단계 복구는 제공하지 않는다. 화면 내 돌아가기·방향 변경·종료를 사용한다.

## 파일과 담당

| 담당 | 이어갈 일 | 현재 연결 지점 |
|---|---|---|
| 사용자 · UI/UX | 카피, 시각 디자인, 필요 선택과 마무리 흐름 피드백 | `src/app/page.tsx`, `src/app/prototype.css` |
| 하늘 · FE | 화면 분할, 실제 API 어댑터 연결, 로딩·오류·재시도 상태, 접근성·성능 점검 | `src/components/need-ui.tsx`, `src/lib/need-flow.ts`, `src/lib/need-events.ts` |
| 효찬 · BE | 합의된 이벤트 수집과 신청 API 구현, 검증·중복 방지·데이터 관리. LLM은 별도 실제 사용 관찰 범위 확정 뒤 검토 | 아래 연동 요구사항 |

## FE–BE 연동 요구사항 초안

아래는 아직 구현하거나 합의하지 않은 요구사항이다. 엔드포인트와 인증 방식은 하늘·효찬이 정한다.

1. **이벤트 수집**: `NeedEvent`의 버전·모드·이벤트 ID·방문 ID·시각·내부 테스트 여부·허용된 속성을 유지한다. 서버는 이벤트 ID로 재전송 중복을 제거하고 스키마와 크기를 검증해야 한다. 실패는 본 체험을 막지 않고 제한된 재시도를 지원한다. 클릭 기록 저장 실패를 신청 실패나 사용자 이탈로 해석하지 않는다.
2. **신청**: 접수를 시작할 때만 실제 신청 범위·일정·개인정보 수집 목적·보관/삭제·동의를 정한다. FE는 서버 확인 후에만 신청 완료를 표시한다. 관심 클릭, 제출 시도, 서버 수락, 실패는 각각 다른 이벤트다. 현재 관심 버튼에 신청 완료를 연결하지 않는다.
3. **LLM**: 현재 코드의 `scripted_demo`와 실제 대화를 다른 버전·모드로 운영한다. 서버 키를 프런트에 넣지 않는다. 임의로 자유 입력란만 추가하지 않는다. 응답 실패·중단·데이터 처리·지원 정보 우회와 실제 관찰 범위를 먼저 정한다.
4. **고지**: 전송이나 저장 위치가 달라지면 사용자의 이용 안내와 기록 안내를 실제 동작에 맞춰 함께 변경한다. 임시 로컬 데이터의 자동 소급 업로드는 하지 않는다.
5. **원문과 계측 분리**: 대화·정리·이메일을 일반 이벤트 속성으로 넣지 않는다. 연구용 원문 취급은 별도 동의와 정책에 따른다.

## 측정에서 유지할 구분

`src/lib/need-events.ts`는 서버 호출 없는 전용 로컬 어댑터다. `mio_need_flow_events_v5` 키에 최대 2,000개를 기록한다. 기존 `mio_counsel_events_v4`를 읽거나 지우지 않는다. 브라우저 저장 차단·삭제·상한·페이지 새 방문에 따라 기록이 누락되거나 방문이 나뉠 수 있다.

| 기록 | 알 수 있는 것 | 이 기록만으로 알 수 없는 것 |
|---|---|---|
| `entry_clicked` | 시작 버튼을 누름 | 고객 문제·실제 사용 |
| `need_selected` | 주어진 선택지 중 고름 | 자연 발생 필요·인구 비율 |
| `demo_option_selected` | 가상 예시 문장 선택 | 직접 작성·실제 자기노출 |
| `summary_downloaded` | 브라우저 다운로드 요청 | 파일 활용·문제 해결 |
| `support_link_clicked` | 외부 공식 정보 링크 클릭 | 상담 문의·예약·방문 |
| `feedback_submitted` | 화면 사용성에 대한 자기보고 | 치료 효과·LLM 효용 |
| `feedback_skipped` | 응답하지 않고 종료 | 부정 평가 |
| `interest_opened` | 다음 체험 준비 안내를 열어봄 | 신청·결제·반복 사용 |

시작률은 같은 방문 ID의 랜딩 노출과 시작 클릭을 연결한다. 분모가 없으면 `null`/`—`이며 0%로 채우지 않는다. 지원 정보 경로를 대화 실패로 분류하지 않는다. 방문 ID는 독립적인 사람 수가 아니다. 프로토타입 내 반복 클릭을 제품 리텐션으로 세지 않는다.

## 지원 링크

2026-09-15 공식 페이지를 확인했다. 지역별 대상·비용·운영시간을 임의로 입력하지 않았다. 외부 이용·예약·상담 결과는 확인하지 않는다.

- 국가정신건강정보포털: https://www.mentalhealth.go.kr/portal/main/index.do
- 보건복지상담센터: https://www.129.go.kr/

## 검증 산출물

2026-09-15 검증 결과: ESLint, TypeScript, Webpack 프로덕션 빌드 통과. 기존 v4 5개 + 새 v5 6개 테스트, 합계 11개 통과. Chrome에서 데스크톱 1440×1050, 모바일 390×844 및 좁은 320×720 화면을 확인했다. 지원 정보 우회, 예시 유지, 수정·다운로드, 부정 응답·미응답 구분, 저장소 차단, 런타임 오류 부재 등을 검증했다. 브라우저 검증·스크린샷은 프로젝트 바깥 `../output/fakedoor-v5/`에 저장했다. 상세는 해당 폴더의 `browser-validation.json`을 확인한다.

이 변경은 기존 문헌·고객 근거 파일을 수정하지 않는다. Phase5의 미확정 사업 가설을 확정하거나 실제 모집·유료화·공개 배포를 실행한 결과가 아니다.
